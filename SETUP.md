# SETUP

This document explains how to set up the RouteMap scaffold locally.

1. Install dependencies with `npm install`.
2. Copy `.env.example` to `.env.local` and fill required values.
3. Start development with `npm run dev`.
